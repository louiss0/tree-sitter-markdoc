---
title: "Longform Blog Series"
author: "Markdoc Example"
---

# Longform Blog Series

{% callout %}
A longform blog with nested block quotes.
{% /callout %}

{% image src="/images/cover.jpg" alt="Cover" /%}

---

## Entry 1

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 1
>
> > Nested quote level two for entry 1
>
> > > Nested quote level three for entry 1

{% note %}
This is a note block for entry 1.
{% /note %}

- Point one for entry 1
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 2

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 2
>
> > Nested quote level two for entry 2
>
> > > Nested quote level three for entry 2

{% note %}
This is a note block for entry 2.
{% /note %}

- Point one for entry 2
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 3

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 3
>
> > Nested quote level two for entry 3
>
> > > Nested quote level three for entry 3

{% note %}
This is a note block for entry 3.
{% /note %}

- Point one for entry 3
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 4

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 4
>
> > Nested quote level two for entry 4
>
> > > Nested quote level three for entry 4

{% note %}
This is a note block for entry 4.
{% /note %}

- Point one for entry 4
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 5

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 5
>
> > Nested quote level two for entry 5
>
> > > Nested quote level three for entry 5

{% note %}
This is a note block for entry 5.
{% /note %}

- Point one for entry 5
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 6

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 6
>
> > Nested quote level two for entry 6
>
> > > Nested quote level three for entry 6

{% note %}
This is a note block for entry 6.
{% /note %}

- Point one for entry 6
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 7

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 7
>
> > Nested quote level two for entry 7
>
> > > Nested quote level three for entry 7

{% note %}
This is a note block for entry 7.
{% /note %}

- Point one for entry 7
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 8

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 8
>
> > Nested quote level two for entry 8
>
> > > Nested quote level three for entry 8

{% note %}
This is a note block for entry 8.
{% /note %}

- Point one for entry 8
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 9

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 9
>
> > Nested quote level two for entry 9
>
> > > Nested quote level three for entry 9

{% note %}
This is a note block for entry 9.
{% /note %}

- Point one for entry 9
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 10

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 10
>
> > Nested quote level two for entry 10
>
> > > Nested quote level three for entry 10

{% note %}
This is a note block for entry 10.
{% /note %}

- Point one for entry 10
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 11

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 11
>
> > Nested quote level two for entry 11
>
> > > Nested quote level three for entry 11

{% note %}
This is a note block for entry 11.
{% /note %}

- Point one for entry 11
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 12

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 12
>
> > Nested quote level two for entry 12
>
> > > Nested quote level three for entry 12

{% note %}
This is a note block for entry 12.
{% /note %}

- Point one for entry 12
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 13

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 13
>
> > Nested quote level two for entry 13
>
> > > Nested quote level three for entry 13

{% note %}
This is a note block for entry 13.
{% /note %}

- Point one for entry 13
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 14

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 14
>
> > Nested quote level two for entry 14
>
> > > Nested quote level three for entry 14

{% note %}
This is a note block for entry 14.
{% /note %}

- Point one for entry 14
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 15

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 15
>
> > Nested quote level two for entry 15
>
> > > Nested quote level three for entry 15

{% note %}
This is a note block for entry 15.
{% /note %}

- Point one for entry 15
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 16

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 16
>
> > Nested quote level two for entry 16
>
> > > Nested quote level three for entry 16

{% note %}
This is a note block for entry 16.
{% /note %}

- Point one for entry 16
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 17

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 17
>
> > Nested quote level two for entry 17
>
> > > Nested quote level three for entry 17

{% note %}
This is a note block for entry 17.
{% /note %}

- Point one for entry 17
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 18

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 18
>
> > Nested quote level two for entry 18
>
> > > Nested quote level three for entry 18

{% note %}
This is a note block for entry 18.
{% /note %}

- Point one for entry 18
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 19

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 19
>
> > Nested quote level two for entry 19
>
> > > Nested quote level three for entry 19

{% note %}
This is a note block for entry 19.
{% /note %}

- Point one for entry 19
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 20

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 20
>
> > Nested quote level two for entry 20
>
> > > Nested quote level three for entry 20

{% note %}
This is a note block for entry 20.
{% /note %}

- Point one for entry 20
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 21

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 21
>
> > Nested quote level two for entry 21
>
> > > Nested quote level three for entry 21

{% note %}
This is a note block for entry 21.
{% /note %}

- Point one for entry 21
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 22

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 22
>
> > Nested quote level two for entry 22
>
> > > Nested quote level three for entry 22

{% note %}
This is a note block for entry 22.
{% /note %}

- Point one for entry 22
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 23

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 23
>
> > Nested quote level two for entry 23
>
> > > Nested quote level three for entry 23

{% note %}
This is a note block for entry 23.
{% /note %}

- Point one for entry 23
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 24

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 24
>
> > Nested quote level two for entry 24
>
> > > Nested quote level three for entry 24

{% note %}
This is a note block for entry 24.
{% /note %}

- Point one for entry 24
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 25

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 25
>
> > Nested quote level two for entry 25
>
> > > Nested quote level three for entry 25

{% note %}
This is a note block for entry 25.
{% /note %}

- Point one for entry 25
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 26

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 26
>
> > Nested quote level two for entry 26
>
> > > Nested quote level three for entry 26

{% note %}
This is a note block for entry 26.
{% /note %}

- Point one for entry 26
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 27

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 27
>
> > Nested quote level two for entry 27
>
> > > Nested quote level three for entry 27

{% note %}
This is a note block for entry 27.
{% /note %}

- Point one for entry 27
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 28

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 28
>
> > Nested quote level two for entry 28
>
> > > Nested quote level three for entry 28

{% note %}
This is a note block for entry 28.
{% /note %}

- Point one for entry 28
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 29

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 29
>
> > Nested quote level two for entry 29
>
> > > Nested quote level three for entry 29

{% note %}
This is a note block for entry 29.
{% /note %}

- Point one for entry 29
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 30

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 30
>
> > Nested quote level two for entry 30
>
> > > Nested quote level three for entry 30

{% note %}
This is a note block for entry 30.
{% /note %}

- Point one for entry 30
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 31

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 31
>
> > Nested quote level two for entry 31
>
> > > Nested quote level three for entry 31

{% note %}
This is a note block for entry 31.
{% /note %}

- Point one for entry 31
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 32

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 32
>
> > Nested quote level two for entry 32
>
> > > Nested quote level three for entry 32

{% note %}
This is a note block for entry 32.
{% /note %}

- Point one for entry 32
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 33

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 33
>
> > Nested quote level two for entry 33
>
> > > Nested quote level three for entry 33

{% note %}
This is a note block for entry 33.
{% /note %}

- Point one for entry 33
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 34

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 34
>
> > Nested quote level two for entry 34
>
> > > Nested quote level three for entry 34

{% note %}
This is a note block for entry 34.
{% /note %}

- Point one for entry 34
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 35

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 35
>
> > Nested quote level two for entry 35
>
> > > Nested quote level three for entry 35

{% note %}
This is a note block for entry 35.
{% /note %}

- Point one for entry 35
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 36

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 36
>
> > Nested quote level two for entry 36
>
> > > Nested quote level three for entry 36

{% note %}
This is a note block for entry 36.
{% /note %}

- Point one for entry 36
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 37

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 37
>
> > Nested quote level two for entry 37
>
> > > Nested quote level three for entry 37

{% note %}
This is a note block for entry 37.
{% /note %}

- Point one for entry 37
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 38

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 38
>
> > Nested quote level two for entry 38
>
> > > Nested quote level three for entry 38

{% note %}
This is a note block for entry 38.
{% /note %}

- Point one for entry 38
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 39

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 39
>
> > Nested quote level two for entry 39
>
> > > Nested quote level three for entry 39

{% note %}
This is a note block for entry 39.
{% /note %}

- Point one for entry 39
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 40

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 40
>
> > Nested quote level two for entry 40
>
> > > Nested quote level three for entry 40

{% note %}
This is a note block for entry 40.
{% /note %}

- Point one for entry 40
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 41

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 41
>
> > Nested quote level two for entry 41
>
> > > Nested quote level three for entry 41

{% note %}
This is a note block for entry 41.
{% /note %}

- Point one for entry 41
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 42

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 42
>
> > Nested quote level two for entry 42
>
> > > Nested quote level three for entry 42

{% note %}
This is a note block for entry 42.
{% /note %}

- Point one for entry 42
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 43

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 43
>
> > Nested quote level two for entry 43
>
> > > Nested quote level three for entry 43

{% note %}
This is a note block for entry 43.
{% /note %}

- Point one for entry 43
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 44

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 44
>
> > Nested quote level two for entry 44
>
> > > Nested quote level three for entry 44

{% note %}
This is a note block for entry 44.
{% /note %}

- Point one for entry 44
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 45

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 45
>
> > Nested quote level two for entry 45
>
> > > Nested quote level three for entry 45

{% note %}
This is a note block for entry 45.
{% /note %}

- Point one for entry 45
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 46

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 46
>
> > Nested quote level two for entry 46
>
> > > Nested quote level three for entry 46

{% note %}
This is a note block for entry 46.
{% /note %}

- Point one for entry 46
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 47

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 47
>
> > Nested quote level two for entry 47
>
> > > Nested quote level three for entry 47

{% note %}
This is a note block for entry 47.
{% /note %}

- Point one for entry 47
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 48

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 48
>
> > Nested quote level two for entry 48
>
> > > Nested quote level three for entry 48

{% note %}
This is a note block for entry 48.
{% /note %}

- Point one for entry 48
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 49

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 49
>
> > Nested quote level two for entry 49
>
> > > Nested quote level three for entry 49

{% note %}
This is a note block for entry 49.
{% /note %}

- Point one for entry 49
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 50

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 50
>
> > Nested quote level two for entry 50
>
> > > Nested quote level three for entry 50

{% note %}
This is a note block for entry 50.
{% /note %}

- Point one for entry 50
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 51

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 51
>
> > Nested quote level two for entry 51
>
> > > Nested quote level three for entry 51

{% note %}
This is a note block for entry 51.
{% /note %}

- Point one for entry 51
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 52

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 52
>
> > Nested quote level two for entry 52
>
> > > Nested quote level three for entry 52

{% note %}
This is a note block for entry 52.
{% /note %}

- Point one for entry 52
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 53

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 53
>
> > Nested quote level two for entry 53
>
> > > Nested quote level three for entry 53

{% note %}
This is a note block for entry 53.
{% /note %}

- Point one for entry 53
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 54

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 54
>
> > Nested quote level two for entry 54
>
> > > Nested quote level three for entry 54

{% note %}
This is a note block for entry 54.
{% /note %}

- Point one for entry 54
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 55

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 55
>
> > Nested quote level two for entry 55
>
> > > Nested quote level three for entry 55

{% note %}
This is a note block for entry 55.
{% /note %}

- Point one for entry 55
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 56

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 56
>
> > Nested quote level two for entry 56
>
> > > Nested quote level three for entry 56

{% note %}
This is a note block for entry 56.
{% /note %}

- Point one for entry 56
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 57

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 57
>
> > Nested quote level two for entry 57
>
> > > Nested quote level three for entry 57

{% note %}
This is a note block for entry 57.
{% /note %}

- Point one for entry 57
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 58

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 58
>
> > Nested quote level two for entry 58
>
> > > Nested quote level three for entry 58

{% note %}
This is a note block for entry 58.
{% /note %}

- Point one for entry 58
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 59

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 59
>
> > Nested quote level two for entry 59
>
> > > Nested quote level three for entry 59

{% note %}
This is a note block for entry 59.
{% /note %}

- Point one for entry 59
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 60

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 60
>
> > Nested quote level two for entry 60
>
> > > Nested quote level three for entry 60

{% note %}
This is a note block for entry 60.
{% /note %}

- Point one for entry 60
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 61

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 61
>
> > Nested quote level two for entry 61
>
> > > Nested quote level three for entry 61

{% note %}
This is a note block for entry 61.
{% /note %}

- Point one for entry 61
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 62

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 62
>
> > Nested quote level two for entry 62
>
> > > Nested quote level three for entry 62

{% note %}
This is a note block for entry 62.
{% /note %}

- Point one for entry 62
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 63

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 63
>
> > Nested quote level two for entry 63
>
> > > Nested quote level three for entry 63

{% note %}
This is a note block for entry 63.
{% /note %}

- Point one for entry 63
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 64

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 64
>
> > Nested quote level two for entry 64
>
> > > Nested quote level three for entry 64

{% note %}
This is a note block for entry 64.
{% /note %}

- Point one for entry 64
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 65

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 65
>
> > Nested quote level two for entry 65
>
> > > Nested quote level three for entry 65

{% note %}
This is a note block for entry 65.
{% /note %}

- Point one for entry 65
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 66

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 66
>
> > Nested quote level two for entry 66
>
> > > Nested quote level three for entry 66

{% note %}
This is a note block for entry 66.
{% /note %}

- Point one for entry 66
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 67

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 67
>
> > Nested quote level two for entry 67
>
> > > Nested quote level three for entry 67

{% note %}
This is a note block for entry 67.
{% /note %}

- Point one for entry 67
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 68

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 68
>
> > Nested quote level two for entry 68
>
> > > Nested quote level three for entry 68

{% note %}
This is a note block for entry 68.
{% /note %}

- Point one for entry 68
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 69

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 69
>
> > Nested quote level two for entry 69
>
> > > Nested quote level three for entry 69

{% note %}
This is a note block for entry 69.
{% /note %}

- Point one for entry 69
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 70

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 70
>
> > Nested quote level two for entry 70
>
> > > Nested quote level three for entry 70

{% note %}
This is a note block for entry 70.
{% /note %}

- Point one for entry 70
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 71

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 71
>
> > Nested quote level two for entry 71
>
> > > Nested quote level three for entry 71

{% note %}
This is a note block for entry 71.
{% /note %}

- Point one for entry 71
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 72

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 72
>
> > Nested quote level two for entry 72
>
> > > Nested quote level three for entry 72

{% note %}
This is a note block for entry 72.
{% /note %}

- Point one for entry 72
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 73

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 73
>
> > Nested quote level two for entry 73
>
> > > Nested quote level three for entry 73

{% note %}
This is a note block for entry 73.
{% /note %}

- Point one for entry 73
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 74

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 74
>
> > Nested quote level two for entry 74
>
> > > Nested quote level three for entry 74

{% note %}
This is a note block for entry 74.
{% /note %}

- Point one for entry 74
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 75

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 75
>
> > Nested quote level two for entry 75
>
> > > Nested quote level three for entry 75

{% note %}
This is a note block for entry 75.
{% /note %}

- Point one for entry 75
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 76

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 76
>
> > Nested quote level two for entry 76
>
> > > Nested quote level three for entry 76

{% note %}
This is a note block for entry 76.
{% /note %}

- Point one for entry 76
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 77

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 77
>
> > Nested quote level two for entry 77
>
> > > Nested quote level three for entry 77

{% note %}
This is a note block for entry 77.
{% /note %}

- Point one for entry 77
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 78

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 78
>
> > Nested quote level two for entry 78
>
> > > Nested quote level three for entry 78

{% note %}
This is a note block for entry 78.
{% /note %}

- Point one for entry 78
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 79

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 79
>
> > Nested quote level two for entry 79
>
> > > Nested quote level three for entry 79

{% note %}
This is a note block for entry 79.
{% /note %}

- Point one for entry 79
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 80

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 80
>
> > Nested quote level two for entry 80
>
> > > Nested quote level three for entry 80

{% note %}
This is a note block for entry 80.
{% /note %}

- Point one for entry 80
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 81

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 81
>
> > Nested quote level two for entry 81
>
> > > Nested quote level three for entry 81

{% note %}
This is a note block for entry 81.
{% /note %}

- Point one for entry 81
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 82

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 82
>
> > Nested quote level two for entry 82
>
> > > Nested quote level three for entry 82

{% note %}
This is a note block for entry 82.
{% /note %}

- Point one for entry 82
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 83

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 83
>
> > Nested quote level two for entry 83
>
> > > Nested quote level three for entry 83

{% note %}
This is a note block for entry 83.
{% /note %}

- Point one for entry 83
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 84

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 84
>
> > Nested quote level two for entry 84
>
> > > Nested quote level three for entry 84

{% note %}
This is a note block for entry 84.
{% /note %}

- Point one for entry 84
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 85

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 85
>
> > Nested quote level two for entry 85
>
> > > Nested quote level three for entry 85

{% note %}
This is a note block for entry 85.
{% /note %}

- Point one for entry 85
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 86

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 86
>
> > Nested quote level two for entry 86
>
> > > Nested quote level three for entry 86

{% note %}
This is a note block for entry 86.
{% /note %}

- Point one for entry 86
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 87

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 87
>
> > Nested quote level two for entry 87
>
> > > Nested quote level three for entry 87

{% note %}
This is a note block for entry 87.
{% /note %}

- Point one for entry 87
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 88

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 88
>
> > Nested quote level two for entry 88
>
> > > Nested quote level three for entry 88

{% note %}
This is a note block for entry 88.
{% /note %}

- Point one for entry 88
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 89

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 89
>
> > Nested quote level two for entry 89
>
> > > Nested quote level three for entry 89

{% note %}
This is a note block for entry 89.
{% /note %}

- Point one for entry 89
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 90

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 90
>
> > Nested quote level two for entry 90
>
> > > Nested quote level three for entry 90

{% note %}
This is a note block for entry 90.
{% /note %}

- Point one for entry 90
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 91

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 91
>
> > Nested quote level two for entry 91
>
> > > Nested quote level three for entry 91

{% note %}
This is a note block for entry 91.
{% /note %}

- Point one for entry 91
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 92

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 92
>
> > Nested quote level two for entry 92
>
> > > Nested quote level three for entry 92

{% note %}
This is a note block for entry 92.
{% /note %}

- Point one for entry 92
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 93

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 93
>
> > Nested quote level two for entry 93
>
> > > Nested quote level three for entry 93

{% note %}
This is a note block for entry 93.
{% /note %}

- Point one for entry 93
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 94

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 94
>
> > Nested quote level two for entry 94
>
> > > Nested quote level three for entry 94

{% note %}
This is a note block for entry 94.
{% /note %}

- Point one for entry 94
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 95

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 95
>
> > Nested quote level two for entry 95
>
> > > Nested quote level three for entry 95

{% note %}
This is a note block for entry 95.
{% /note %}

- Point one for entry 95
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 96

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 96
>
> > Nested quote level two for entry 96
>
> > > Nested quote level three for entry 96

{% note %}
This is a note block for entry 96.
{% /note %}

- Point one for entry 96
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 97

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 97
>
> > Nested quote level two for entry 97
>
> > > Nested quote level three for entry 97

{% note %}
This is a note block for entry 97.
{% /note %}

- Point one for entry 97
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 98

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 98
>
> > Nested quote level two for entry 98
>
> > > Nested quote level three for entry 98

{% note %}
This is a note block for entry 98.
{% /note %}

- Point one for entry 98
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 99

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 99
>
> > Nested quote level two for entry 99
>
> > > Nested quote level three for entry 99

{% note %}
This is a note block for entry 99.
{% /note %}

- Point one for entry 99
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 100

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 100
>
> > Nested quote level two for entry 100
>
> > > Nested quote level three for entry 100

{% note %}
This is a note block for entry 100.
{% /note %}

- Point one for entry 100
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 101

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 101
>
> > Nested quote level two for entry 101
>
> > > Nested quote level three for entry 101

{% note %}
This is a note block for entry 101.
{% /note %}

- Point one for entry 101
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 102

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 102
>
> > Nested quote level two for entry 102
>
> > > Nested quote level three for entry 102

{% note %}
This is a note block for entry 102.
{% /note %}

- Point one for entry 102
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 103

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 103
>
> > Nested quote level two for entry 103
>
> > > Nested quote level three for entry 103

{% note %}
This is a note block for entry 103.
{% /note %}

- Point one for entry 103
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 104

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 104
>
> > Nested quote level two for entry 104
>
> > > Nested quote level three for entry 104

{% note %}
This is a note block for entry 104.
{% /note %}

- Point one for entry 104
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 105

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 105
>
> > Nested quote level two for entry 105
>
> > > Nested quote level three for entry 105

{% note %}
This is a note block for entry 105.
{% /note %}

- Point one for entry 105
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 106

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 106
>
> > Nested quote level two for entry 106
>
> > > Nested quote level three for entry 106

{% note %}
This is a note block for entry 106.
{% /note %}

- Point one for entry 106
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 107

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 107
>
> > Nested quote level two for entry 107
>
> > > Nested quote level three for entry 107

{% note %}
This is a note block for entry 107.
{% /note %}

- Point one for entry 107
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 108

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 108
>
> > Nested quote level two for entry 108
>
> > > Nested quote level three for entry 108

{% note %}
This is a note block for entry 108.
{% /note %}

- Point one for entry 108
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 109

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 109
>
> > Nested quote level two for entry 109
>
> > > Nested quote level three for entry 109

{% note %}
This is a note block for entry 109.
{% /note %}

- Point one for entry 109
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 110

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 110
>
> > Nested quote level two for entry 110
>
> > > Nested quote level three for entry 110

{% note %}
This is a note block for entry 110.
{% /note %}

- Point one for entry 110
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 111

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 111
>
> > Nested quote level two for entry 111
>
> > > Nested quote level three for entry 111

{% note %}
This is a note block for entry 111.
{% /note %}

- Point one for entry 111
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 112

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 112
>
> > Nested quote level two for entry 112
>
> > > Nested quote level three for entry 112

{% note %}
This is a note block for entry 112.
{% /note %}

- Point one for entry 112
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 113

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 113
>
> > Nested quote level two for entry 113
>
> > > Nested quote level three for entry 113

{% note %}
This is a note block for entry 113.
{% /note %}

- Point one for entry 113
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 114

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 114
>
> > Nested quote level two for entry 114
>
> > > Nested quote level three for entry 114

{% note %}
This is a note block for entry 114.
{% /note %}

- Point one for entry 114
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 115

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 115
>
> > Nested quote level two for entry 115
>
> > > Nested quote level three for entry 115

{% note %}
This is a note block for entry 115.
{% /note %}

- Point one for entry 115
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 116

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 116
>
> > Nested quote level two for entry 116
>
> > > Nested quote level three for entry 116

{% note %}
This is a note block for entry 116.
{% /note %}

- Point one for entry 116
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 117

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 117
>
> > Nested quote level two for entry 117
>
> > > Nested quote level three for entry 117

{% note %}
This is a note block for entry 117.
{% /note %}

- Point one for entry 117
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 118

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 118
>
> > Nested quote level two for entry 118
>
> > > Nested quote level three for entry 118

{% note %}
This is a note block for entry 118.
{% /note %}

- Point one for entry 118
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 119

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 119
>
> > Nested quote level two for entry 119
>
> > > Nested quote level three for entry 119

{% note %}
This is a note block for entry 119.
{% /note %}

- Point one for entry 119
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 120

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 120
>
> > Nested quote level two for entry 120
>
> > > Nested quote level three for entry 120

{% note %}
This is a note block for entry 120.
{% /note %}

- Point one for entry 120
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 121

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 121
>
> > Nested quote level two for entry 121
>
> > > Nested quote level three for entry 121

{% note %}
This is a note block for entry 121.
{% /note %}

- Point one for entry 121
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 122

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 122
>
> > Nested quote level two for entry 122
>
> > > Nested quote level three for entry 122

{% note %}
This is a note block for entry 122.
{% /note %}

- Point one for entry 122
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 123

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 123
>
> > Nested quote level two for entry 123
>
> > > Nested quote level three for entry 123

{% note %}
This is a note block for entry 123.
{% /note %}

- Point one for entry 123
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 124

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 124
>
> > Nested quote level two for entry 124
>
> > > Nested quote level three for entry 124

{% note %}
This is a note block for entry 124.
{% /note %}

- Point one for entry 124
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 125

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 125
>
> > Nested quote level two for entry 125
>
> > > Nested quote level three for entry 125

{% note %}
This is a note block for entry 125.
{% /note %}

- Point one for entry 125
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 126

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 126
>
> > Nested quote level two for entry 126
>
> > > Nested quote level three for entry 126

{% note %}
This is a note block for entry 126.
{% /note %}

- Point one for entry 126
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 127

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 127
>
> > Nested quote level two for entry 127
>
> > > Nested quote level three for entry 127

{% note %}
This is a note block for entry 127.
{% /note %}

- Point one for entry 127
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 128

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 128
>
> > Nested quote level two for entry 128
>
> > > Nested quote level three for entry 128

{% note %}
This is a note block for entry 128.
{% /note %}

- Point one for entry 128
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 129

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 129
>
> > Nested quote level two for entry 129
>
> > > Nested quote level three for entry 129

{% note %}
This is a note block for entry 129.
{% /note %}

- Point one for entry 129
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 130

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 130
>
> > Nested quote level two for entry 130
>
> > > Nested quote level three for entry 130

{% note %}
This is a note block for entry 130.
{% /note %}

- Point one for entry 130
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 131

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 131
>
> > Nested quote level two for entry 131
>
> > > Nested quote level three for entry 131

{% note %}
This is a note block for entry 131.
{% /note %}

- Point one for entry 131
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 132

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 132
>
> > Nested quote level two for entry 132
>
> > > Nested quote level three for entry 132

{% note %}
This is a note block for entry 132.
{% /note %}

- Point one for entry 132
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 133

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 133
>
> > Nested quote level two for entry 133
>
> > > Nested quote level three for entry 133

{% note %}
This is a note block for entry 133.
{% /note %}

- Point one for entry 133
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 134

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 134
>
> > Nested quote level two for entry 134
>
> > > Nested quote level three for entry 134

{% note %}
This is a note block for entry 134.
{% /note %}

- Point one for entry 134
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 135

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 135
>
> > Nested quote level two for entry 135
>
> > > Nested quote level three for entry 135

{% note %}
This is a note block for entry 135.
{% /note %}

- Point one for entry 135
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 136

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 136
>
> > Nested quote level two for entry 136
>
> > > Nested quote level three for entry 136

{% note %}
This is a note block for entry 136.
{% /note %}

- Point one for entry 136
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 137

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 137
>
> > Nested quote level two for entry 137
>
> > > Nested quote level three for entry 137

{% note %}
This is a note block for entry 137.
{% /note %}

- Point one for entry 137
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 138

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 138
>
> > Nested quote level two for entry 138
>
> > > Nested quote level three for entry 138

{% note %}
This is a note block for entry 138.
{% /note %}

- Point one for entry 138
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 139

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 139
>
> > Nested quote level two for entry 139
>
> > > Nested quote level three for entry 139

{% note %}
This is a note block for entry 139.
{% /note %}

- Point one for entry 139
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 140

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 140
>
> > Nested quote level two for entry 140
>
> > > Nested quote level three for entry 140

{% note %}
This is a note block for entry 140.
{% /note %}

- Point one for entry 140
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 141

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 141
>
> > Nested quote level two for entry 141
>
> > > Nested quote level three for entry 141

{% note %}
This is a note block for entry 141.
{% /note %}

- Point one for entry 141
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 142

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 142
>
> > Nested quote level two for entry 142
>
> > > Nested quote level three for entry 142

{% note %}
This is a note block for entry 142.
{% /note %}

- Point one for entry 142
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 143

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 143
>
> > Nested quote level two for entry 143
>
> > > Nested quote level three for entry 143

{% note %}
This is a note block for entry 143.
{% /note %}

- Point one for entry 143
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 144

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 144
>
> > Nested quote level two for entry 144
>
> > > Nested quote level three for entry 144

{% note %}
This is a note block for entry 144.
{% /note %}

- Point one for entry 144
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 145

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 145
>
> > Nested quote level two for entry 145
>
> > > Nested quote level three for entry 145

{% note %}
This is a note block for entry 145.
{% /note %}

- Point one for entry 145
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 146

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 146
>
> > Nested quote level two for entry 146
>
> > > Nested quote level three for entry 146

{% note %}
This is a note block for entry 146.
{% /note %}

- Point one for entry 146
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 147

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 147
>
> > Nested quote level two for entry 147
>
> > > Nested quote level three for entry 147

{% note %}
This is a note block for entry 147.
{% /note %}

- Point one for entry 147
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 148

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 148
>
> > Nested quote level two for entry 148
>
> > > Nested quote level three for entry 148

{% note %}
This is a note block for entry 148.
{% /note %}

- Point one for entry 148
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 149

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 149
>
> > Nested quote level two for entry 149
>
> > > Nested quote level three for entry 149

{% note %}
This is a note block for entry 149.
{% /note %}

- Point one for entry 149
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 150

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 150
>
> > Nested quote level two for entry 150
>
> > > Nested quote level three for entry 150

{% note %}
This is a note block for entry 150.
{% /note %}

- Point one for entry 150
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 151

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 151
>
> > Nested quote level two for entry 151
>
> > > Nested quote level three for entry 151

{% note %}
This is a note block for entry 151.
{% /note %}

- Point one for entry 151
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 152

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 152
>
> > Nested quote level two for entry 152
>
> > > Nested quote level three for entry 152

{% note %}
This is a note block for entry 152.
{% /note %}

- Point one for entry 152
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 153

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 153
>
> > Nested quote level two for entry 153
>
> > > Nested quote level three for entry 153

{% note %}
This is a note block for entry 153.
{% /note %}

- Point one for entry 153
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 154

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 154
>
> > Nested quote level two for entry 154
>
> > > Nested quote level three for entry 154

{% note %}
This is a note block for entry 154.
{% /note %}

- Point one for entry 154
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 155

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 155
>
> > Nested quote level two for entry 155
>
> > > Nested quote level three for entry 155

{% note %}
This is a note block for entry 155.
{% /note %}

- Point one for entry 155
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 156

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 156
>
> > Nested quote level two for entry 156
>
> > > Nested quote level three for entry 156

{% note %}
This is a note block for entry 156.
{% /note %}

- Point one for entry 156
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 157

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 157
>
> > Nested quote level two for entry 157
>
> > > Nested quote level three for entry 157

{% note %}
This is a note block for entry 157.
{% /note %}

- Point one for entry 157
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 158

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 158
>
> > Nested quote level two for entry 158
>
> > > Nested quote level three for entry 158

{% note %}
This is a note block for entry 158.
{% /note %}

- Point one for entry 158
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 159

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 159
>
> > Nested quote level two for entry 159
>
> > > Nested quote level three for entry 159

{% note %}
This is a note block for entry 159.
{% /note %}

- Point one for entry 159
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 160

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 160
>
> > Nested quote level two for entry 160
>
> > > Nested quote level three for entry 160

{% note %}
This is a note block for entry 160.
{% /note %}

- Point one for entry 160
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 161

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 161
>
> > Nested quote level two for entry 161
>
> > > Nested quote level three for entry 161

{% note %}
This is a note block for entry 161.
{% /note %}

- Point one for entry 161
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 162

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 162
>
> > Nested quote level two for entry 162
>
> > > Nested quote level three for entry 162

{% note %}
This is a note block for entry 162.
{% /note %}

- Point one for entry 162
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 163

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 163
>
> > Nested quote level two for entry 163
>
> > > Nested quote level three for entry 163

{% note %}
This is a note block for entry 163.
{% /note %}

- Point one for entry 163
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 164

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 164
>
> > Nested quote level two for entry 164
>
> > > Nested quote level three for entry 164

{% note %}
This is a note block for entry 164.
{% /note %}

- Point one for entry 164
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 165

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 165
>
> > Nested quote level two for entry 165
>
> > > Nested quote level three for entry 165

{% note %}
This is a note block for entry 165.
{% /note %}

- Point one for entry 165
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 166

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 166
>
> > Nested quote level two for entry 166
>
> > > Nested quote level three for entry 166

{% note %}
This is a note block for entry 166.
{% /note %}

- Point one for entry 166
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 167

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 167
>
> > Nested quote level two for entry 167
>
> > > Nested quote level three for entry 167

{% note %}
This is a note block for entry 167.
{% /note %}

- Point one for entry 167
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 168

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 168
>
> > Nested quote level two for entry 168
>
> > > Nested quote level three for entry 168

{% note %}
This is a note block for entry 168.
{% /note %}

- Point one for entry 168
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 169

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 169
>
> > Nested quote level two for entry 169
>
> > > Nested quote level three for entry 169

{% note %}
This is a note block for entry 169.
{% /note %}

- Point one for entry 169
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 170

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 170
>
> > Nested quote level two for entry 170
>
> > > Nested quote level three for entry 170

{% note %}
This is a note block for entry 170.
{% /note %}

- Point one for entry 170
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 171

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 171
>
> > Nested quote level two for entry 171
>
> > > Nested quote level three for entry 171

{% note %}
This is a note block for entry 171.
{% /note %}

- Point one for entry 171
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 172

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 172
>
> > Nested quote level two for entry 172
>
> > > Nested quote level three for entry 172

{% note %}
This is a note block for entry 172.
{% /note %}

- Point one for entry 172
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 173

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 173
>
> > Nested quote level two for entry 173
>
> > > Nested quote level three for entry 173

{% note %}
This is a note block for entry 173.
{% /note %}

- Point one for entry 173
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 174

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 174
>
> > Nested quote level two for entry 174
>
> > > Nested quote level three for entry 174

{% note %}
This is a note block for entry 174.
{% /note %}

- Point one for entry 174
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 175

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 175
>
> > Nested quote level two for entry 175
>
> > > Nested quote level three for entry 175

{% note %}
This is a note block for entry 175.
{% /note %}

- Point one for entry 175
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 176

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 176
>
> > Nested quote level two for entry 176
>
> > > Nested quote level three for entry 176

{% note %}
This is a note block for entry 176.
{% /note %}

- Point one for entry 176
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 177

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 177
>
> > Nested quote level two for entry 177
>
> > > Nested quote level three for entry 177

{% note %}
This is a note block for entry 177.
{% /note %}

- Point one for entry 177
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 178

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 178
>
> > Nested quote level two for entry 178
>
> > > Nested quote level three for entry 178

{% note %}
This is a note block for entry 178.
{% /note %}

- Point one for entry 178
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 179

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 179
>
> > Nested quote level two for entry 179
>
> > > Nested quote level three for entry 179

{% note %}
This is a note block for entry 179.
{% /note %}

- Point one for entry 179
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 180

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 180
>
> > Nested quote level two for entry 180
>
> > > Nested quote level three for entry 180

{% note %}
This is a note block for entry 180.
{% /note %}

- Point one for entry 180
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 181

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 181
>
> > Nested quote level two for entry 181
>
> > > Nested quote level three for entry 181

{% note %}
This is a note block for entry 181.
{% /note %}

- Point one for entry 181
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 182

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 182
>
> > Nested quote level two for entry 182
>
> > > Nested quote level three for entry 182

{% note %}
This is a note block for entry 182.
{% /note %}

- Point one for entry 182
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 183

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 183
>
> > Nested quote level two for entry 183
>
> > > Nested quote level three for entry 183

{% note %}
This is a note block for entry 183.
{% /note %}

- Point one for entry 183
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 184

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 184
>
> > Nested quote level two for entry 184
>
> > > Nested quote level three for entry 184

{% note %}
This is a note block for entry 184.
{% /note %}

- Point one for entry 184
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 185

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 185
>
> > Nested quote level two for entry 185
>
> > > Nested quote level three for entry 185

{% note %}
This is a note block for entry 185.
{% /note %}

- Point one for entry 185
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 186

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 186
>
> > Nested quote level two for entry 186
>
> > > Nested quote level three for entry 186

{% note %}
This is a note block for entry 186.
{% /note %}

- Point one for entry 186
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 187

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 187
>
> > Nested quote level two for entry 187
>
> > > Nested quote level three for entry 187

{% note %}
This is a note block for entry 187.
{% /note %}

- Point one for entry 187
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 188

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 188
>
> > Nested quote level two for entry 188
>
> > > Nested quote level three for entry 188

{% note %}
This is a note block for entry 188.
{% /note %}

- Point one for entry 188
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 189

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 189
>
> > Nested quote level two for entry 189
>
> > > Nested quote level three for entry 189

{% note %}
This is a note block for entry 189.
{% /note %}

- Point one for entry 189
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 190

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 190
>
> > Nested quote level two for entry 190
>
> > > Nested quote level three for entry 190

{% note %}
This is a note block for entry 190.
{% /note %}

- Point one for entry 190
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 191

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 191
>
> > Nested quote level two for entry 191
>
> > > Nested quote level three for entry 191

{% note %}
This is a note block for entry 191.
{% /note %}

- Point one for entry 191
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 192

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 192
>
> > Nested quote level two for entry 192
>
> > > Nested quote level three for entry 192

{% note %}
This is a note block for entry 192.
{% /note %}

- Point one for entry 192
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 193

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 193
>
> > Nested quote level two for entry 193
>
> > > Nested quote level three for entry 193

{% note %}
This is a note block for entry 193.
{% /note %}

- Point one for entry 193
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 194

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 194
>
> > Nested quote level two for entry 194
>
> > > Nested quote level three for entry 194

{% note %}
This is a note block for entry 194.
{% /note %}

- Point one for entry 194
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 195

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 195
>
> > Nested quote level two for entry 195
>
> > > Nested quote level three for entry 195

{% note %}
This is a note block for entry 195.
{% /note %}

- Point one for entry 195
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 196

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 196
>
> > Nested quote level two for entry 196
>
> > > Nested quote level three for entry 196

{% note %}
This is a note block for entry 196.
{% /note %}

- Point one for entry 196
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 197

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 197
>
> > Nested quote level two for entry 197
>
> > > Nested quote level three for entry 197

{% note %}
This is a note block for entry 197.
{% /note %}

- Point one for entry 197
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 198

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 198
>
> > Nested quote level two for entry 198
>
> > > Nested quote level three for entry 198

{% note %}
This is a note block for entry 198.
{% /note %}

- Point one for entry 198
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 199

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 199
>
> > Nested quote level two for entry 199
>
> > > Nested quote level three for entry 199

{% note %}
This is a note block for entry 199.
{% /note %}

- Point one for entry 199
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 200

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 200
>
> > Nested quote level two for entry 200
>
> > > Nested quote level three for entry 200

{% note %}
This is a note block for entry 200.
{% /note %}

- Point one for entry 200
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 201

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 201
>
> > Nested quote level two for entry 201
>
> > > Nested quote level three for entry 201

{% note %}
This is a note block for entry 201.
{% /note %}

- Point one for entry 201
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 202

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 202
>
> > Nested quote level two for entry 202
>
> > > Nested quote level three for entry 202

{% note %}
This is a note block for entry 202.
{% /note %}

- Point one for entry 202
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 203

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 203
>
> > Nested quote level two for entry 203
>
> > > Nested quote level three for entry 203

{% note %}
This is a note block for entry 203.
{% /note %}

- Point one for entry 203
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 204

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 204
>
> > Nested quote level two for entry 204
>
> > > Nested quote level three for entry 204

{% note %}
This is a note block for entry 204.
{% /note %}

- Point one for entry 204
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 205

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 205
>
> > Nested quote level two for entry 205
>
> > > Nested quote level three for entry 205

{% note %}
This is a note block for entry 205.
{% /note %}

- Point one for entry 205
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 206

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 206
>
> > Nested quote level two for entry 206
>
> > > Nested quote level three for entry 206

{% note %}
This is a note block for entry 206.
{% /note %}

- Point one for entry 206
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 207

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 207
>
> > Nested quote level two for entry 207
>
> > > Nested quote level three for entry 207

{% note %}
This is a note block for entry 207.
{% /note %}

- Point one for entry 207
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 208

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 208
>
> > Nested quote level two for entry 208
>
> > > Nested quote level three for entry 208

{% note %}
This is a note block for entry 208.
{% /note %}

- Point one for entry 208
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 209

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 209
>
> > Nested quote level two for entry 209
>
> > > Nested quote level three for entry 209

{% note %}
This is a note block for entry 209.
{% /note %}

- Point one for entry 209
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 210

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 210
>
> > Nested quote level two for entry 210
>
> > > Nested quote level three for entry 210

{% note %}
This is a note block for entry 210.
{% /note %}

- Point one for entry 210
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 211

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 211
>
> > Nested quote level two for entry 211
>
> > > Nested quote level three for entry 211

{% note %}
This is a note block for entry 211.
{% /note %}

- Point one for entry 211
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 212

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 212
>
> > Nested quote level two for entry 212
>
> > > Nested quote level three for entry 212

{% note %}
This is a note block for entry 212.
{% /note %}

- Point one for entry 212
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 213

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 213
>
> > Nested quote level two for entry 213
>
> > > Nested quote level three for entry 213

{% note %}
This is a note block for entry 213.
{% /note %}

- Point one for entry 213
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 214

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 214
>
> > Nested quote level two for entry 214
>
> > > Nested quote level three for entry 214

{% note %}
This is a note block for entry 214.
{% /note %}

- Point one for entry 214
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 215

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 215
>
> > Nested quote level two for entry 215
>
> > > Nested quote level three for entry 215

{% note %}
This is a note block for entry 215.
{% /note %}

- Point one for entry 215
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 216

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 216
>
> > Nested quote level two for entry 216
>
> > > Nested quote level three for entry 216

{% note %}
This is a note block for entry 216.
{% /note %}

- Point one for entry 216
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 217

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 217
>
> > Nested quote level two for entry 217
>
> > > Nested quote level three for entry 217

{% note %}
This is a note block for entry 217.
{% /note %}

- Point one for entry 217
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 218

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 218
>
> > Nested quote level two for entry 218
>
> > > Nested quote level three for entry 218

{% note %}
This is a note block for entry 218.
{% /note %}

- Point one for entry 218
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 219

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 219
>
> > Nested quote level two for entry 219
>
> > > Nested quote level three for entry 219

{% note %}
This is a note block for entry 219.
{% /note %}

- Point one for entry 219
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 220

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 220
>
> > Nested quote level two for entry 220
>
> > > Nested quote level three for entry 220

{% note %}
This is a note block for entry 220.
{% /note %}

- Point one for entry 220
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 221

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 221
>
> > Nested quote level two for entry 221
>
> > > Nested quote level three for entry 221

{% note %}
This is a note block for entry 221.
{% /note %}

- Point one for entry 221
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 222

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 222
>
> > Nested quote level two for entry 222
>
> > > Nested quote level three for entry 222

{% note %}
This is a note block for entry 222.
{% /note %}

- Point one for entry 222
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 223

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 223
>
> > Nested quote level two for entry 223
>
> > > Nested quote level three for entry 223

{% note %}
This is a note block for entry 223.
{% /note %}

- Point one for entry 223
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 224

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 224
>
> > Nested quote level two for entry 224
>
> > > Nested quote level three for entry 224

{% note %}
This is a note block for entry 224.
{% /note %}

- Point one for entry 224
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 225

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 225
>
> > Nested quote level two for entry 225
>
> > > Nested quote level three for entry 225

{% note %}
This is a note block for entry 225.
{% /note %}

- Point one for entry 225
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 226

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 226
>
> > Nested quote level two for entry 226
>
> > > Nested quote level three for entry 226

{% note %}
This is a note block for entry 226.
{% /note %}

- Point one for entry 226
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 227

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 227
>
> > Nested quote level two for entry 227
>
> > > Nested quote level three for entry 227

{% note %}
This is a note block for entry 227.
{% /note %}

- Point one for entry 227
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 228

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 228
>
> > Nested quote level two for entry 228
>
> > > Nested quote level three for entry 228

{% note %}
This is a note block for entry 228.
{% /note %}

- Point one for entry 228
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 229

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 229
>
> > Nested quote level two for entry 229
>
> > > Nested quote level three for entry 229

{% note %}
This is a note block for entry 229.
{% /note %}

- Point one for entry 229
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 230

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 230
>
> > Nested quote level two for entry 230
>
> > > Nested quote level three for entry 230

{% note %}
This is a note block for entry 230.
{% /note %}

- Point one for entry 230
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 231

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 231
>
> > Nested quote level two for entry 231
>
> > > Nested quote level three for entry 231

{% note %}
This is a note block for entry 231.
{% /note %}

- Point one for entry 231
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 232

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 232
>
> > Nested quote level two for entry 232
>
> > > Nested quote level three for entry 232

{% note %}
This is a note block for entry 232.
{% /note %}

- Point one for entry 232
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 233

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 233
>
> > Nested quote level two for entry 233
>
> > > Nested quote level three for entry 233

{% note %}
This is a note block for entry 233.
{% /note %}

- Point one for entry 233
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 234

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 234
>
> > Nested quote level two for entry 234
>
> > > Nested quote level three for entry 234

{% note %}
This is a note block for entry 234.
{% /note %}

- Point one for entry 234
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 235

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 235
>
> > Nested quote level two for entry 235
>
> > > Nested quote level three for entry 235

{% note %}
This is a note block for entry 235.
{% /note %}

- Point one for entry 235
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 236

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 236
>
> > Nested quote level two for entry 236
>
> > > Nested quote level three for entry 236

{% note %}
This is a note block for entry 236.
{% /note %}

- Point one for entry 236
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 237

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 237
>
> > Nested quote level two for entry 237
>
> > > Nested quote level three for entry 237

{% note %}
This is a note block for entry 237.
{% /note %}

- Point one for entry 237
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 238

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 238
>
> > Nested quote level two for entry 238
>
> > > Nested quote level three for entry 238

{% note %}
This is a note block for entry 238.
{% /note %}

- Point one for entry 238
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 239

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 239
>
> > Nested quote level two for entry 239
>
> > > Nested quote level three for entry 239

{% note %}
This is a note block for entry 239.
{% /note %}

- Point one for entry 239
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 240

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 240
>
> > Nested quote level two for entry 240
>
> > > Nested quote level three for entry 240

{% note %}
This is a note block for entry 240.
{% /note %}

- Point one for entry 240
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 241

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 241
>
> > Nested quote level two for entry 241
>
> > > Nested quote level three for entry 241

{% note %}
This is a note block for entry 241.
{% /note %}

- Point one for entry 241
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 242

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 242
>
> > Nested quote level two for entry 242
>
> > > Nested quote level three for entry 242

{% note %}
This is a note block for entry 242.
{% /note %}

- Point one for entry 242
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 243

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 243
>
> > Nested quote level two for entry 243
>
> > > Nested quote level three for entry 243

{% note %}
This is a note block for entry 243.
{% /note %}

- Point one for entry 243
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 244

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 244
>
> > Nested quote level two for entry 244
>
> > > Nested quote level three for entry 244

{% note %}
This is a note block for entry 244.
{% /note %}

- Point one for entry 244
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 245

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 245
>
> > Nested quote level two for entry 245
>
> > > Nested quote level three for entry 245

{% note %}
This is a note block for entry 245.
{% /note %}

- Point one for entry 245
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 246

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 246
>
> > Nested quote level two for entry 246
>
> > > Nested quote level three for entry 246

{% note %}
This is a note block for entry 246.
{% /note %}

- Point one for entry 246
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 247

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 247
>
> > Nested quote level two for entry 247
>
> > > Nested quote level three for entry 247

{% note %}
This is a note block for entry 247.
{% /note %}

- Point one for entry 247
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 248

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 248
>
> > Nested quote level two for entry 248
>
> > > Nested quote level three for entry 248

{% note %}
This is a note block for entry 248.
{% /note %}

- Point one for entry 248
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 249

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 249
>
> > Nested quote level two for entry 249
>
> > > Nested quote level three for entry 249

{% note %}
This is a note block for entry 249.
{% /note %}

- Point one for entry 249
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 250

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 250
>
> > Nested quote level two for entry 250
>
> > > Nested quote level three for entry 250

{% note %}
This is a note block for entry 250.
{% /note %}

- Point one for entry 250
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 251

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 251
>
> > Nested quote level two for entry 251
>
> > > Nested quote level three for entry 251

{% note %}
This is a note block for entry 251.
{% /note %}

- Point one for entry 251
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 252

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 252
>
> > Nested quote level two for entry 252
>
> > > Nested quote level three for entry 252

{% note %}
This is a note block for entry 252.
{% /note %}

- Point one for entry 252
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 253

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 253
>
> > Nested quote level two for entry 253
>
> > > Nested quote level three for entry 253

{% note %}
This is a note block for entry 253.
{% /note %}

- Point one for entry 253
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 254

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 254
>
> > Nested quote level two for entry 254
>
> > > Nested quote level three for entry 254

{% note %}
This is a note block for entry 254.
{% /note %}

- Point one for entry 254
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 255

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 255
>
> > Nested quote level two for entry 255
>
> > > Nested quote level three for entry 255

{% note %}
This is a note block for entry 255.
{% /note %}

- Point one for entry 255
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 256

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 256
>
> > Nested quote level two for entry 256
>
> > > Nested quote level three for entry 256

{% note %}
This is a note block for entry 256.
{% /note %}

- Point one for entry 256
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 257

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 257
>
> > Nested quote level two for entry 257
>
> > > Nested quote level three for entry 257

{% note %}
This is a note block for entry 257.
{% /note %}

- Point one for entry 257
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 258

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 258
>
> > Nested quote level two for entry 258
>
> > > Nested quote level three for entry 258

{% note %}
This is a note block for entry 258.
{% /note %}

- Point one for entry 258
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 259

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 259
>
> > Nested quote level two for entry 259
>
> > > Nested quote level three for entry 259

{% note %}
This is a note block for entry 259.
{% /note %}

- Point one for entry 259
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 260

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 260
>
> > Nested quote level two for entry 260
>
> > > Nested quote level three for entry 260

{% note %}
This is a note block for entry 260.
{% /note %}

- Point one for entry 260
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 261

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 261
>
> > Nested quote level two for entry 261
>
> > > Nested quote level three for entry 261

{% note %}
This is a note block for entry 261.
{% /note %}

- Point one for entry 261
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 262

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 262
>
> > Nested quote level two for entry 262
>
> > > Nested quote level three for entry 262

{% note %}
This is a note block for entry 262.
{% /note %}

- Point one for entry 262
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 263

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 263
>
> > Nested quote level two for entry 263
>
> > > Nested quote level three for entry 263

{% note %}
This is a note block for entry 263.
{% /note %}

- Point one for entry 263
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 264

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 264
>
> > Nested quote level two for entry 264
>
> > > Nested quote level three for entry 264

{% note %}
This is a note block for entry 264.
{% /note %}

- Point one for entry 264
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 265

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 265
>
> > Nested quote level two for entry 265
>
> > > Nested quote level three for entry 265

{% note %}
This is a note block for entry 265.
{% /note %}

- Point one for entry 265
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 266

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 266
>
> > Nested quote level two for entry 266
>
> > > Nested quote level three for entry 266

{% note %}
This is a note block for entry 266.
{% /note %}

- Point one for entry 266
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 267

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 267
>
> > Nested quote level two for entry 267
>
> > > Nested quote level three for entry 267

{% note %}
This is a note block for entry 267.
{% /note %}

- Point one for entry 267
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 268

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 268
>
> > Nested quote level two for entry 268
>
> > > Nested quote level three for entry 268

{% note %}
This is a note block for entry 268.
{% /note %}

- Point one for entry 268
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 269

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 269
>
> > Nested quote level two for entry 269
>
> > > Nested quote level three for entry 269

{% note %}
This is a note block for entry 269.
{% /note %}

- Point one for entry 269
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 270

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 270
>
> > Nested quote level two for entry 270
>
> > > Nested quote level three for entry 270

{% note %}
This is a note block for entry 270.
{% /note %}

- Point one for entry 270
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 271

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 271
>
> > Nested quote level two for entry 271
>
> > > Nested quote level three for entry 271

{% note %}
This is a note block for entry 271.
{% /note %}

- Point one for entry 271
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 272

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 272
>
> > Nested quote level two for entry 272
>
> > > Nested quote level three for entry 272

{% note %}
This is a note block for entry 272.
{% /note %}

- Point one for entry 272
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 273

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 273
>
> > Nested quote level two for entry 273
>
> > > Nested quote level three for entry 273

{% note %}
This is a note block for entry 273.
{% /note %}

- Point one for entry 273
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 274

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 274
>
> > Nested quote level two for entry 274
>
> > > Nested quote level three for entry 274

{% note %}
This is a note block for entry 274.
{% /note %}

- Point one for entry 274
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 275

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 275
>
> > Nested quote level two for entry 275
>
> > > Nested quote level three for entry 275

{% note %}
This is a note block for entry 275.
{% /note %}

- Point one for entry 275
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 276

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 276
>
> > Nested quote level two for entry 276
>
> > > Nested quote level three for entry 276

{% note %}
This is a note block for entry 276.
{% /note %}

- Point one for entry 276
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 277

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 277
>
> > Nested quote level two for entry 277
>
> > > Nested quote level three for entry 277

{% note %}
This is a note block for entry 277.
{% /note %}

- Point one for entry 277
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 278

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 278
>
> > Nested quote level two for entry 278
>
> > > Nested quote level three for entry 278

{% note %}
This is a note block for entry 278.
{% /note %}

- Point one for entry 278
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 279

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 279
>
> > Nested quote level two for entry 279
>
> > > Nested quote level three for entry 279

{% note %}
This is a note block for entry 279.
{% /note %}

- Point one for entry 279
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 280

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 280
>
> > Nested quote level two for entry 280
>
> > > Nested quote level three for entry 280

{% note %}
This is a note block for entry 280.
{% /note %}

- Point one for entry 280
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 281

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 281
>
> > Nested quote level two for entry 281
>
> > > Nested quote level three for entry 281

{% note %}
This is a note block for entry 281.
{% /note %}

- Point one for entry 281
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 282

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 282
>
> > Nested quote level two for entry 282
>
> > > Nested quote level three for entry 282

{% note %}
This is a note block for entry 282.
{% /note %}

- Point one for entry 282
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 283

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 283
>
> > Nested quote level two for entry 283
>
> > > Nested quote level three for entry 283

{% note %}
This is a note block for entry 283.
{% /note %}

- Point one for entry 283
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 284

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 284
>
> > Nested quote level two for entry 284
>
> > > Nested quote level three for entry 284

{% note %}
This is a note block for entry 284.
{% /note %}

- Point one for entry 284
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 285

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 285
>
> > Nested quote level two for entry 285
>
> > > Nested quote level three for entry 285

{% note %}
This is a note block for entry 285.
{% /note %}

- Point one for entry 285
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 286

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 286
>
> > Nested quote level two for entry 286
>
> > > Nested quote level three for entry 286

{% note %}
This is a note block for entry 286.
{% /note %}

- Point one for entry 286
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 287

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 287
>
> > Nested quote level two for entry 287
>
> > > Nested quote level three for entry 287

{% note %}
This is a note block for entry 287.
{% /note %}

- Point one for entry 287
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 288

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 288
>
> > Nested quote level two for entry 288
>
> > > Nested quote level three for entry 288

{% note %}
This is a note block for entry 288.
{% /note %}

- Point one for entry 288
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 289

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 289
>
> > Nested quote level two for entry 289
>
> > > Nested quote level three for entry 289

{% note %}
This is a note block for entry 289.
{% /note %}

- Point one for entry 289
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 290

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 290
>
> > Nested quote level two for entry 290
>
> > > Nested quote level three for entry 290

{% note %}
This is a note block for entry 290.
{% /note %}

- Point one for entry 290
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 291

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 291
>
> > Nested quote level two for entry 291
>
> > > Nested quote level three for entry 291

{% note %}
This is a note block for entry 291.
{% /note %}

- Point one for entry 291
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 292

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 292
>
> > Nested quote level two for entry 292
>
> > > Nested quote level three for entry 292

{% note %}
This is a note block for entry 292.
{% /note %}

- Point one for entry 292
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 293

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 293
>
> > Nested quote level two for entry 293
>
> > > Nested quote level three for entry 293

{% note %}
This is a note block for entry 293.
{% /note %}

- Point one for entry 293
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 294

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 294
>
> > Nested quote level two for entry 294
>
> > > Nested quote level three for entry 294

{% note %}
This is a note block for entry 294.
{% /note %}

- Point one for entry 294
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 295

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 295
>
> > Nested quote level two for entry 295
>
> > > Nested quote level three for entry 295

{% note %}
This is a note block for entry 295.
{% /note %}

- Point one for entry 295
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 296

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 296
>
> > Nested quote level two for entry 296
>
> > > Nested quote level three for entry 296

{% note %}
This is a note block for entry 296.
{% /note %}

- Point one for entry 296
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 297

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 297
>
> > Nested quote level two for entry 297
>
> > > Nested quote level three for entry 297

{% note %}
This is a note block for entry 297.
{% /note %}

- Point one for entry 297
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 298

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 298
>
> > Nested quote level two for entry 298
>
> > > Nested quote level three for entry 298

{% note %}
This is a note block for entry 298.
{% /note %}

- Point one for entry 298
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 299

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 299
>
> > Nested quote level two for entry 299
>
> > > Nested quote level three for entry 299

{% note %}
This is a note block for entry 299.
{% /note %}

- Point one for entry 299
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 300

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 300
>
> > Nested quote level two for entry 300
>
> > > Nested quote level three for entry 300

{% note %}
This is a note block for entry 300.
{% /note %}

- Point one for entry 300
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 301

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 301
>
> > Nested quote level two for entry 301
>
> > > Nested quote level three for entry 301

{% note %}
This is a note block for entry 301.
{% /note %}

- Point one for entry 301
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 302

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 302
>
> > Nested quote level two for entry 302
>
> > > Nested quote level three for entry 302

{% note %}
This is a note block for entry 302.
{% /note %}

- Point one for entry 302
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 303

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 303
>
> > Nested quote level two for entry 303
>
> > > Nested quote level three for entry 303

{% note %}
This is a note block for entry 303.
{% /note %}

- Point one for entry 303
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 304

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 304
>
> > Nested quote level two for entry 304
>
> > > Nested quote level three for entry 304

{% note %}
This is a note block for entry 304.
{% /note %}

- Point one for entry 304
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 305

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 305
>
> > Nested quote level two for entry 305
>
> > > Nested quote level three for entry 305

{% note %}
This is a note block for entry 305.
{% /note %}

- Point one for entry 305
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 306

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 306
>
> > Nested quote level two for entry 306
>
> > > Nested quote level three for entry 306

{% note %}
This is a note block for entry 306.
{% /note %}

- Point one for entry 306
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 307

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 307
>
> > Nested quote level two for entry 307
>
> > > Nested quote level three for entry 307

{% note %}
This is a note block for entry 307.
{% /note %}

- Point one for entry 307
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 308

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 308
>
> > Nested quote level two for entry 308
>
> > > Nested quote level three for entry 308

{% note %}
This is a note block for entry 308.
{% /note %}

- Point one for entry 308
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 309

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 309
>
> > Nested quote level two for entry 309
>
> > > Nested quote level three for entry 309

{% note %}
This is a note block for entry 309.
{% /note %}

- Point one for entry 309
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 310

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 310
>
> > Nested quote level two for entry 310
>
> > > Nested quote level three for entry 310

{% note %}
This is a note block for entry 310.
{% /note %}

- Point one for entry 310
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 311

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 311
>
> > Nested quote level two for entry 311
>
> > > Nested quote level three for entry 311

{% note %}
This is a note block for entry 311.
{% /note %}

- Point one for entry 311
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 312

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 312
>
> > Nested quote level two for entry 312
>
> > > Nested quote level three for entry 312

{% note %}
This is a note block for entry 312.
{% /note %}

- Point one for entry 312
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 313

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 313
>
> > Nested quote level two for entry 313
>
> > > Nested quote level three for entry 313

{% note %}
This is a note block for entry 313.
{% /note %}

- Point one for entry 313
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 314

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 314
>
> > Nested quote level two for entry 314
>
> > > Nested quote level three for entry 314

{% note %}
This is a note block for entry 314.
{% /note %}

- Point one for entry 314
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 315

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 315
>
> > Nested quote level two for entry 315
>
> > > Nested quote level three for entry 315

{% note %}
This is a note block for entry 315.
{% /note %}

- Point one for entry 315
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 316

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 316
>
> > Nested quote level two for entry 316
>
> > > Nested quote level three for entry 316

{% note %}
This is a note block for entry 316.
{% /note %}

- Point one for entry 316
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 317

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 317
>
> > Nested quote level two for entry 317
>
> > > Nested quote level three for entry 317

{% note %}
This is a note block for entry 317.
{% /note %}

- Point one for entry 317
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 318

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 318
>
> > Nested quote level two for entry 318
>
> > > Nested quote level three for entry 318

{% note %}
This is a note block for entry 318.
{% /note %}

- Point one for entry 318
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 319

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 319
>
> > Nested quote level two for entry 319
>
> > > Nested quote level three for entry 319

{% note %}
This is a note block for entry 319.
{% /note %}

- Point one for entry 319
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 320

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 320
>
> > Nested quote level two for entry 320
>
> > > Nested quote level three for entry 320

{% note %}
This is a note block for entry 320.
{% /note %}

- Point one for entry 320
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 321

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 321
>
> > Nested quote level two for entry 321
>
> > > Nested quote level three for entry 321

{% note %}
This is a note block for entry 321.
{% /note %}

- Point one for entry 321
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 322

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 322
>
> > Nested quote level two for entry 322
>
> > > Nested quote level three for entry 322

{% note %}
This is a note block for entry 322.
{% /note %}

- Point one for entry 322
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 323

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 323
>
> > Nested quote level two for entry 323
>
> > > Nested quote level three for entry 323

{% note %}
This is a note block for entry 323.
{% /note %}

- Point one for entry 323
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 324

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 324
>
> > Nested quote level two for entry 324
>
> > > Nested quote level three for entry 324

{% note %}
This is a note block for entry 324.
{% /note %}

- Point one for entry 324
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 325

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 325
>
> > Nested quote level two for entry 325
>
> > > Nested quote level three for entry 325

{% note %}
This is a note block for entry 325.
{% /note %}

- Point one for entry 325
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 326

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 326
>
> > Nested quote level two for entry 326
>
> > > Nested quote level three for entry 326

{% note %}
This is a note block for entry 326.
{% /note %}

- Point one for entry 326
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 327

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 327
>
> > Nested quote level two for entry 327
>
> > > Nested quote level three for entry 327

{% note %}
This is a note block for entry 327.
{% /note %}

- Point one for entry 327
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 328

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 328
>
> > Nested quote level two for entry 328
>
> > > Nested quote level three for entry 328

{% note %}
This is a note block for entry 328.
{% /note %}

- Point one for entry 328
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 329

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 329
>
> > Nested quote level two for entry 329
>
> > > Nested quote level three for entry 329

{% note %}
This is a note block for entry 329.
{% /note %}

- Point one for entry 329
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 330

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 330
>
> > Nested quote level two for entry 330
>
> > > Nested quote level three for entry 330

{% note %}
This is a note block for entry 330.
{% /note %}

- Point one for entry 330
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 331

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 331
>
> > Nested quote level two for entry 331
>
> > > Nested quote level three for entry 331

{% note %}
This is a note block for entry 331.
{% /note %}

- Point one for entry 331
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 332

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 332
>
> > Nested quote level two for entry 332
>
> > > Nested quote level three for entry 332

{% note %}
This is a note block for entry 332.
{% /note %}

- Point one for entry 332
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 333

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 333
>
> > Nested quote level two for entry 333
>
> > > Nested quote level three for entry 333

{% note %}
This is a note block for entry 333.
{% /note %}

- Point one for entry 333
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 334

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 334
>
> > Nested quote level two for entry 334
>
> > > Nested quote level three for entry 334

{% note %}
This is a note block for entry 334.
{% /note %}

- Point one for entry 334
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 335

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 335
>
> > Nested quote level two for entry 335
>
> > > Nested quote level three for entry 335

{% note %}
This is a note block for entry 335.
{% /note %}

- Point one for entry 335
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 336

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 336
>
> > Nested quote level two for entry 336
>
> > > Nested quote level three for entry 336

{% note %}
This is a note block for entry 336.
{% /note %}

- Point one for entry 336
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 337

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 337
>
> > Nested quote level two for entry 337
>
> > > Nested quote level three for entry 337

{% note %}
This is a note block for entry 337.
{% /note %}

- Point one for entry 337
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 338

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 338
>
> > Nested quote level two for entry 338
>
> > > Nested quote level three for entry 338

{% note %}
This is a note block for entry 338.
{% /note %}

- Point one for entry 338
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 339

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 339
>
> > Nested quote level two for entry 339
>
> > > Nested quote level three for entry 339

{% note %}
This is a note block for entry 339.
{% /note %}

- Point one for entry 339
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 340

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 340
>
> > Nested quote level two for entry 340
>
> > > Nested quote level three for entry 340

{% note %}
This is a note block for entry 340.
{% /note %}

- Point one for entry 340
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 341

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 341
>
> > Nested quote level two for entry 341
>
> > > Nested quote level three for entry 341

{% note %}
This is a note block for entry 341.
{% /note %}

- Point one for entry 341
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 342

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 342
>
> > Nested quote level two for entry 342
>
> > > Nested quote level three for entry 342

{% note %}
This is a note block for entry 342.
{% /note %}

- Point one for entry 342
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 343

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 343
>
> > Nested quote level two for entry 343
>
> > > Nested quote level three for entry 343

{% note %}
This is a note block for entry 343.
{% /note %}

- Point one for entry 343
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 344

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 344
>
> > Nested quote level two for entry 344
>
> > > Nested quote level three for entry 344

{% note %}
This is a note block for entry 344.
{% /note %}

- Point one for entry 344
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 345

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 345
>
> > Nested quote level two for entry 345
>
> > > Nested quote level three for entry 345

{% note %}
This is a note block for entry 345.
{% /note %}

- Point one for entry 345
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 346

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 346
>
> > Nested quote level two for entry 346
>
> > > Nested quote level three for entry 346

{% note %}
This is a note block for entry 346.
{% /note %}

- Point one for entry 346
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 347

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 347
>
> > Nested quote level two for entry 347
>
> > > Nested quote level three for entry 347

{% note %}
This is a note block for entry 347.
{% /note %}

- Point one for entry 347
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 348

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 348
>
> > Nested quote level two for entry 348
>
> > > Nested quote level three for entry 348

{% note %}
This is a note block for entry 348.
{% /note %}

- Point one for entry 348
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 349

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 349
>
> > Nested quote level two for entry 349
>
> > > Nested quote level three for entry 349

{% note %}
This is a note block for entry 349.
{% /note %}

- Point one for entry 349
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 350

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 350
>
> > Nested quote level two for entry 350
>
> > > Nested quote level three for entry 350

{% note %}
This is a note block for entry 350.
{% /note %}

- Point one for entry 350
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 351

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 351
>
> > Nested quote level two for entry 351
>
> > > Nested quote level three for entry 351

{% note %}
This is a note block for entry 351.
{% /note %}

- Point one for entry 351
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 352

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 352
>
> > Nested quote level two for entry 352
>
> > > Nested quote level three for entry 352

{% note %}
This is a note block for entry 352.
{% /note %}

- Point one for entry 352
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 353

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 353
>
> > Nested quote level two for entry 353
>
> > > Nested quote level three for entry 353

{% note %}
This is a note block for entry 353.
{% /note %}

- Point one for entry 353
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 354

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 354
>
> > Nested quote level two for entry 354
>
> > > Nested quote level three for entry 354

{% note %}
This is a note block for entry 354.
{% /note %}

- Point one for entry 354
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 355

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 355
>
> > Nested quote level two for entry 355
>
> > > Nested quote level three for entry 355

{% note %}
This is a note block for entry 355.
{% /note %}

- Point one for entry 355
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 356

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 356
>
> > Nested quote level two for entry 356
>
> > > Nested quote level three for entry 356

{% note %}
This is a note block for entry 356.
{% /note %}

- Point one for entry 356
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 357

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 357
>
> > Nested quote level two for entry 357
>
> > > Nested quote level three for entry 357

{% note %}
This is a note block for entry 357.
{% /note %}

- Point one for entry 357
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 358

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 358
>
> > Nested quote level two for entry 358
>
> > > Nested quote level three for entry 358

{% note %}
This is a note block for entry 358.
{% /note %}

- Point one for entry 358
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 359

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 359
>
> > Nested quote level two for entry 359
>
> > > Nested quote level three for entry 359

{% note %}
This is a note block for entry 359.
{% /note %}

- Point one for entry 359
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 360

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 360
>
> > Nested quote level two for entry 360
>
> > > Nested quote level three for entry 360

{% note %}
This is a note block for entry 360.
{% /note %}

- Point one for entry 360
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 361

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 361
>
> > Nested quote level two for entry 361
>
> > > Nested quote level three for entry 361

{% note %}
This is a note block for entry 361.
{% /note %}

- Point one for entry 361
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 362

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 362
>
> > Nested quote level two for entry 362
>
> > > Nested quote level three for entry 362

{% note %}
This is a note block for entry 362.
{% /note %}

- Point one for entry 362
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 363

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 363
>
> > Nested quote level two for entry 363
>
> > > Nested quote level three for entry 363

{% note %}
This is a note block for entry 363.
{% /note %}

- Point one for entry 363
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 364

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 364
>
> > Nested quote level two for entry 364
>
> > > Nested quote level three for entry 364

{% note %}
This is a note block for entry 364.
{% /note %}

- Point one for entry 364
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 365

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 365
>
> > Nested quote level two for entry 365
>
> > > Nested quote level three for entry 365

{% note %}
This is a note block for entry 365.
{% /note %}

- Point one for entry 365
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 366

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 366
>
> > Nested quote level two for entry 366
>
> > > Nested quote level three for entry 366

{% note %}
This is a note block for entry 366.
{% /note %}

- Point one for entry 366
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 367

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 367
>
> > Nested quote level two for entry 367
>
> > > Nested quote level three for entry 367

{% note %}
This is a note block for entry 367.
{% /note %}

- Point one for entry 367
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 368

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 368
>
> > Nested quote level two for entry 368
>
> > > Nested quote level three for entry 368

{% note %}
This is a note block for entry 368.
{% /note %}

- Point one for entry 368
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 369

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 369
>
> > Nested quote level two for entry 369
>
> > > Nested quote level three for entry 369

{% note %}
This is a note block for entry 369.
{% /note %}

- Point one for entry 369
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 370

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 370
>
> > Nested quote level two for entry 370
>
> > > Nested quote level three for entry 370

{% note %}
This is a note block for entry 370.
{% /note %}

- Point one for entry 370
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 371

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 371
>
> > Nested quote level two for entry 371
>
> > > Nested quote level three for entry 371

{% note %}
This is a note block for entry 371.
{% /note %}

- Point one for entry 371
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 372

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 372
>
> > Nested quote level two for entry 372
>
> > > Nested quote level three for entry 372

{% note %}
This is a note block for entry 372.
{% /note %}

- Point one for entry 372
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 373

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 373
>
> > Nested quote level two for entry 373
>
> > > Nested quote level three for entry 373

{% note %}
This is a note block for entry 373.
{% /note %}

- Point one for entry 373
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 374

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 374
>
> > Nested quote level two for entry 374
>
> > > Nested quote level three for entry 374

{% note %}
This is a note block for entry 374.
{% /note %}

- Point one for entry 374
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 375

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 375
>
> > Nested quote level two for entry 375
>
> > > Nested quote level three for entry 375

{% note %}
This is a note block for entry 375.
{% /note %}

- Point one for entry 375
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 376

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 376
>
> > Nested quote level two for entry 376
>
> > > Nested quote level three for entry 376

{% note %}
This is a note block for entry 376.
{% /note %}

- Point one for entry 376
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 377

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 377
>
> > Nested quote level two for entry 377
>
> > > Nested quote level three for entry 377

{% note %}
This is a note block for entry 377.
{% /note %}

- Point one for entry 377
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 378

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 378
>
> > Nested quote level two for entry 378
>
> > > Nested quote level three for entry 378

{% note %}
This is a note block for entry 378.
{% /note %}

- Point one for entry 378
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 379

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 379
>
> > Nested quote level two for entry 379
>
> > > Nested quote level three for entry 379

{% note %}
This is a note block for entry 379.
{% /note %}

- Point one for entry 379
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 380

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 380
>
> > Nested quote level two for entry 380
>
> > > Nested quote level three for entry 380

{% note %}
This is a note block for entry 380.
{% /note %}

- Point one for entry 380
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 381

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 381
>
> > Nested quote level two for entry 381
>
> > > Nested quote level three for entry 381

{% note %}
This is a note block for entry 381.
{% /note %}

- Point one for entry 381
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 382

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 382
>
> > Nested quote level two for entry 382
>
> > > Nested quote level three for entry 382

{% note %}
This is a note block for entry 382.
{% /note %}

- Point one for entry 382
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 383

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 383
>
> > Nested quote level two for entry 383
>
> > > Nested quote level three for entry 383

{% note %}
This is a note block for entry 383.
{% /note %}

- Point one for entry 383
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 384

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 384
>
> > Nested quote level two for entry 384
>
> > > Nested quote level three for entry 384

{% note %}
This is a note block for entry 384.
{% /note %}

- Point one for entry 384
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 385

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 385
>
> > Nested quote level two for entry 385
>
> > > Nested quote level three for entry 385

{% note %}
This is a note block for entry 385.
{% /note %}

- Point one for entry 385
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 386

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 386
>
> > Nested quote level two for entry 386
>
> > > Nested quote level three for entry 386

{% note %}
This is a note block for entry 386.
{% /note %}

- Point one for entry 386
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 387

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 387
>
> > Nested quote level two for entry 387
>
> > > Nested quote level three for entry 387

{% note %}
This is a note block for entry 387.
{% /note %}

- Point one for entry 387
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 388

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 388
>
> > Nested quote level two for entry 388
>
> > > Nested quote level three for entry 388

{% note %}
This is a note block for entry 388.
{% /note %}

- Point one for entry 388
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 389

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 389
>
> > Nested quote level two for entry 389
>
> > > Nested quote level three for entry 389

{% note %}
This is a note block for entry 389.
{% /note %}

- Point one for entry 389
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 390

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 390
>
> > Nested quote level two for entry 390
>
> > > Nested quote level three for entry 390

{% note %}
This is a note block for entry 390.
{% /note %}

- Point one for entry 390
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 391

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 391
>
> > Nested quote level two for entry 391
>
> > > Nested quote level three for entry 391

{% note %}
This is a note block for entry 391.
{% /note %}

- Point one for entry 391
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 392

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 392
>
> > Nested quote level two for entry 392
>
> > > Nested quote level three for entry 392

{% note %}
This is a note block for entry 392.
{% /note %}

- Point one for entry 392
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 393

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 393
>
> > Nested quote level two for entry 393
>
> > > Nested quote level three for entry 393

{% note %}
This is a note block for entry 393.
{% /note %}

- Point one for entry 393
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 394

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 394
>
> > Nested quote level two for entry 394
>
> > > Nested quote level three for entry 394

{% note %}
This is a note block for entry 394.
{% /note %}

- Point one for entry 394
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 395

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 395
>
> > Nested quote level two for entry 395
>
> > > Nested quote level three for entry 395

{% note %}
This is a note block for entry 395.
{% /note %}

- Point one for entry 395
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 396

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 396
>
> > Nested quote level two for entry 396
>
> > > Nested quote level three for entry 396

{% note %}
This is a note block for entry 396.
{% /note %}

- Point one for entry 396
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 397

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 397
>
> > Nested quote level two for entry 397
>
> > > Nested quote level three for entry 397

{% note %}
This is a note block for entry 397.
{% /note %}

- Point one for entry 397
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 398

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 398
>
> > Nested quote level two for entry 398
>
> > > Nested quote level three for entry 398

{% note %}
This is a note block for entry 398.
{% /note %}

- Point one for entry 398
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 399

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 399
>
> > Nested quote level two for entry 399
>
> > > Nested quote level three for entry 399

{% note %}
This is a note block for entry 399.
{% /note %}

- Point one for entry 399
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 400

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 400
>
> > Nested quote level two for entry 400
>
> > > Nested quote level three for entry 400

{% note %}
This is a note block for entry 400.
{% /note %}

- Point one for entry 400
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 401

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 401
>
> > Nested quote level two for entry 401
>
> > > Nested quote level three for entry 401

{% note %}
This is a note block for entry 401.
{% /note %}

- Point one for entry 401
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 402

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 402
>
> > Nested quote level two for entry 402
>
> > > Nested quote level three for entry 402

{% note %}
This is a note block for entry 402.
{% /note %}

- Point one for entry 402
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 403

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 403
>
> > Nested quote level two for entry 403
>
> > > Nested quote level three for entry 403

{% note %}
This is a note block for entry 403.
{% /note %}

- Point one for entry 403
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 404

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 404
>
> > Nested quote level two for entry 404
>
> > > Nested quote level three for entry 404

{% note %}
This is a note block for entry 404.
{% /note %}

- Point one for entry 404
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 405

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 405
>
> > Nested quote level two for entry 405
>
> > > Nested quote level three for entry 405

{% note %}
This is a note block for entry 405.
{% /note %}

- Point one for entry 405
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 406

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 406
>
> > Nested quote level two for entry 406
>
> > > Nested quote level three for entry 406

{% note %}
This is a note block for entry 406.
{% /note %}

- Point one for entry 406
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 407

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 407
>
> > Nested quote level two for entry 407
>
> > > Nested quote level three for entry 407

{% note %}
This is a note block for entry 407.
{% /note %}

- Point one for entry 407
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 408

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 408
>
> > Nested quote level two for entry 408
>
> > > Nested quote level three for entry 408

{% note %}
This is a note block for entry 408.
{% /note %}

- Point one for entry 408
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 409

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 409
>
> > Nested quote level two for entry 409
>
> > > Nested quote level three for entry 409

{% note %}
This is a note block for entry 409.
{% /note %}

- Point one for entry 409
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 410

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 410
>
> > Nested quote level two for entry 410
>
> > > Nested quote level three for entry 410

{% note %}
This is a note block for entry 410.
{% /note %}

- Point one for entry 410
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 411

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 411
>
> > Nested quote level two for entry 411
>
> > > Nested quote level three for entry 411

{% note %}
This is a note block for entry 411.
{% /note %}

- Point one for entry 411
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 412

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 412
>
> > Nested quote level two for entry 412
>
> > > Nested quote level three for entry 412

{% note %}
This is a note block for entry 412.
{% /note %}

- Point one for entry 412
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 413

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 413
>
> > Nested quote level two for entry 413
>
> > > Nested quote level three for entry 413

{% note %}
This is a note block for entry 413.
{% /note %}

- Point one for entry 413
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 414

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 414
>
> > Nested quote level two for entry 414
>
> > > Nested quote level three for entry 414

{% note %}
This is a note block for entry 414.
{% /note %}

- Point one for entry 414
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 415

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 415
>
> > Nested quote level two for entry 415
>
> > > Nested quote level three for entry 415

{% note %}
This is a note block for entry 415.
{% /note %}

- Point one for entry 415
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 416

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 416
>
> > Nested quote level two for entry 416
>
> > > Nested quote level three for entry 416

{% note %}
This is a note block for entry 416.
{% /note %}

- Point one for entry 416
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 417

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 417
>
> > Nested quote level two for entry 417
>
> > > Nested quote level three for entry 417

{% note %}
This is a note block for entry 417.
{% /note %}

- Point one for entry 417
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 418

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 418
>
> > Nested quote level two for entry 418
>
> > > Nested quote level three for entry 418

{% note %}
This is a note block for entry 418.
{% /note %}

- Point one for entry 418
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 419

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 419
>
> > Nested quote level two for entry 419
>
> > > Nested quote level three for entry 419

{% note %}
This is a note block for entry 419.
{% /note %}

- Point one for entry 419
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 420

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 420
>
> > Nested quote level two for entry 420
>
> > > Nested quote level three for entry 420

{% note %}
This is a note block for entry 420.
{% /note %}

- Point one for entry 420
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 421

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 421
>
> > Nested quote level two for entry 421
>
> > > Nested quote level three for entry 421

{% note %}
This is a note block for entry 421.
{% /note %}

- Point one for entry 421
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 422

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 422
>
> > Nested quote level two for entry 422
>
> > > Nested quote level three for entry 422

{% note %}
This is a note block for entry 422.
{% /note %}

- Point one for entry 422
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 423

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 423
>
> > Nested quote level two for entry 423
>
> > > Nested quote level three for entry 423

{% note %}
This is a note block for entry 423.
{% /note %}

- Point one for entry 423
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 424

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 424
>
> > Nested quote level two for entry 424
>
> > > Nested quote level three for entry 424

{% note %}
This is a note block for entry 424.
{% /note %}

- Point one for entry 424
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 425

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 425
>
> > Nested quote level two for entry 425
>
> > > Nested quote level three for entry 425

{% note %}
This is a note block for entry 425.
{% /note %}

- Point one for entry 425
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 426

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 426
>
> > Nested quote level two for entry 426
>
> > > Nested quote level three for entry 426

{% note %}
This is a note block for entry 426.
{% /note %}

- Point one for entry 426
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 427

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 427
>
> > Nested quote level two for entry 427
>
> > > Nested quote level three for entry 427

{% note %}
This is a note block for entry 427.
{% /note %}

- Point one for entry 427
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 428

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 428
>
> > Nested quote level two for entry 428
>
> > > Nested quote level three for entry 428

{% note %}
This is a note block for entry 428.
{% /note %}

- Point one for entry 428
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 429

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 429
>
> > Nested quote level two for entry 429
>
> > > Nested quote level three for entry 429

{% note %}
This is a note block for entry 429.
{% /note %}

- Point one for entry 429
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 430

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 430
>
> > Nested quote level two for entry 430
>
> > > Nested quote level three for entry 430

{% note %}
This is a note block for entry 430.
{% /note %}

- Point one for entry 430
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 431

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 431
>
> > Nested quote level two for entry 431
>
> > > Nested quote level three for entry 431

{% note %}
This is a note block for entry 431.
{% /note %}

- Point one for entry 431
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 432

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 432
>
> > Nested quote level two for entry 432
>
> > > Nested quote level three for entry 432

{% note %}
This is a note block for entry 432.
{% /note %}

- Point one for entry 432
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 433

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 433
>
> > Nested quote level two for entry 433
>
> > > Nested quote level three for entry 433

{% note %}
This is a note block for entry 433.
{% /note %}

- Point one for entry 433
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 434

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 434
>
> > Nested quote level two for entry 434
>
> > > Nested quote level three for entry 434

{% note %}
This is a note block for entry 434.
{% /note %}

- Point one for entry 434
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 435

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 435
>
> > Nested quote level two for entry 435
>
> > > Nested quote level three for entry 435

{% note %}
This is a note block for entry 435.
{% /note %}

- Point one for entry 435
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 436

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 436
>
> > Nested quote level two for entry 436
>
> > > Nested quote level three for entry 436

{% note %}
This is a note block for entry 436.
{% /note %}

- Point one for entry 436
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 437

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 437
>
> > Nested quote level two for entry 437
>
> > > Nested quote level three for entry 437

{% note %}
This is a note block for entry 437.
{% /note %}

- Point one for entry 437
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 438

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 438
>
> > Nested quote level two for entry 438
>
> > > Nested quote level three for entry 438

{% note %}
This is a note block for entry 438.
{% /note %}

- Point one for entry 438
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 439

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 439
>
> > Nested quote level two for entry 439
>
> > > Nested quote level three for entry 439

{% note %}
This is a note block for entry 439.
{% /note %}

- Point one for entry 439
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 440

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 440
>
> > Nested quote level two for entry 440
>
> > > Nested quote level three for entry 440

{% note %}
This is a note block for entry 440.
{% /note %}

- Point one for entry 440
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 441

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 441
>
> > Nested quote level two for entry 441
>
> > > Nested quote level three for entry 441

{% note %}
This is a note block for entry 441.
{% /note %}

- Point one for entry 441
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 442

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 442
>
> > Nested quote level two for entry 442
>
> > > Nested quote level three for entry 442

{% note %}
This is a note block for entry 442.
{% /note %}

- Point one for entry 442
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 443

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 443
>
> > Nested quote level two for entry 443
>
> > > Nested quote level three for entry 443

{% note %}
This is a note block for entry 443.
{% /note %}

- Point one for entry 443
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 444

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 444
>
> > Nested quote level two for entry 444
>
> > > Nested quote level three for entry 444

{% note %}
This is a note block for entry 444.
{% /note %}

- Point one for entry 444
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 445

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 445
>
> > Nested quote level two for entry 445
>
> > > Nested quote level three for entry 445

{% note %}
This is a note block for entry 445.
{% /note %}

- Point one for entry 445
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 446

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 446
>
> > Nested quote level two for entry 446
>
> > > Nested quote level three for entry 446

{% note %}
This is a note block for entry 446.
{% /note %}

- Point one for entry 446
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 447

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 447
>
> > Nested quote level two for entry 447
>
> > > Nested quote level three for entry 447

{% note %}
This is a note block for entry 447.
{% /note %}

- Point one for entry 447
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 448

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 448
>
> > Nested quote level two for entry 448
>
> > > Nested quote level three for entry 448

{% note %}
This is a note block for entry 448.
{% /note %}

- Point one for entry 448
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 449

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 449
>
> > Nested quote level two for entry 449
>
> > > Nested quote level three for entry 449

{% note %}
This is a note block for entry 449.
{% /note %}

- Point one for entry 449
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 450

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 450
>
> > Nested quote level two for entry 450
>
> > > Nested quote level three for entry 450

{% note %}
This is a note block for entry 450.
{% /note %}

- Point one for entry 450
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 451

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 451
>
> > Nested quote level two for entry 451
>
> > > Nested quote level three for entry 451

{% note %}
This is a note block for entry 451.
{% /note %}

- Point one for entry 451
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 452

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 452
>
> > Nested quote level two for entry 452
>
> > > Nested quote level three for entry 452

{% note %}
This is a note block for entry 452.
{% /note %}

- Point one for entry 452
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 453

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 453
>
> > Nested quote level two for entry 453
>
> > > Nested quote level three for entry 453

{% note %}
This is a note block for entry 453.
{% /note %}

- Point one for entry 453
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 454

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 454
>
> > Nested quote level two for entry 454
>
> > > Nested quote level three for entry 454

{% note %}
This is a note block for entry 454.
{% /note %}

- Point one for entry 454
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 455

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 455
>
> > Nested quote level two for entry 455
>
> > > Nested quote level three for entry 455

{% note %}
This is a note block for entry 455.
{% /note %}

- Point one for entry 455
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 456

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 456
>
> > Nested quote level two for entry 456
>
> > > Nested quote level three for entry 456

{% note %}
This is a note block for entry 456.
{% /note %}

- Point one for entry 456
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 457

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 457
>
> > Nested quote level two for entry 457
>
> > > Nested quote level three for entry 457

{% note %}
This is a note block for entry 457.
{% /note %}

- Point one for entry 457
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 458

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 458
>
> > Nested quote level two for entry 458
>
> > > Nested quote level three for entry 458

{% note %}
This is a note block for entry 458.
{% /note %}

- Point one for entry 458
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 459

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 459
>
> > Nested quote level two for entry 459
>
> > > Nested quote level three for entry 459

{% note %}
This is a note block for entry 459.
{% /note %}

- Point one for entry 459
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 460

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 460
>
> > Nested quote level two for entry 460
>
> > > Nested quote level three for entry 460

{% note %}
This is a note block for entry 460.
{% /note %}

- Point one for entry 460
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 461

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 461
>
> > Nested quote level two for entry 461
>
> > > Nested quote level three for entry 461

{% note %}
This is a note block for entry 461.
{% /note %}

- Point one for entry 461
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 462

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 462
>
> > Nested quote level two for entry 462
>
> > > Nested quote level three for entry 462

{% note %}
This is a note block for entry 462.
{% /note %}

- Point one for entry 462
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 463

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 463
>
> > Nested quote level two for entry 463
>
> > > Nested quote level three for entry 463

{% note %}
This is a note block for entry 463.
{% /note %}

- Point one for entry 463
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 464

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 464
>
> > Nested quote level two for entry 464
>
> > > Nested quote level three for entry 464

{% note %}
This is a note block for entry 464.
{% /note %}

- Point one for entry 464
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 465

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 465
>
> > Nested quote level two for entry 465
>
> > > Nested quote level three for entry 465

{% note %}
This is a note block for entry 465.
{% /note %}

- Point one for entry 465
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 466

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 466
>
> > Nested quote level two for entry 466
>
> > > Nested quote level three for entry 466

{% note %}
This is a note block for entry 466.
{% /note %}

- Point one for entry 466
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 467

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 467
>
> > Nested quote level two for entry 467
>
> > > Nested quote level three for entry 467

{% note %}
This is a note block for entry 467.
{% /note %}

- Point one for entry 467
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 468

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 468
>
> > Nested quote level two for entry 468
>
> > > Nested quote level three for entry 468

{% note %}
This is a note block for entry 468.
{% /note %}

- Point one for entry 468
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 469

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 469
>
> > Nested quote level two for entry 469
>
> > > Nested quote level three for entry 469

{% note %}
This is a note block for entry 469.
{% /note %}

- Point one for entry 469
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 470

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 470
>
> > Nested quote level two for entry 470
>
> > > Nested quote level three for entry 470

{% note %}
This is a note block for entry 470.
{% /note %}

- Point one for entry 470
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 471

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 471
>
> > Nested quote level two for entry 471
>
> > > Nested quote level three for entry 471

{% note %}
This is a note block for entry 471.
{% /note %}

- Point one for entry 471
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 472

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 472
>
> > Nested quote level two for entry 472
>
> > > Nested quote level three for entry 472

{% note %}
This is a note block for entry 472.
{% /note %}

- Point one for entry 472
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 473

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 473
>
> > Nested quote level two for entry 473
>
> > > Nested quote level three for entry 473

{% note %}
This is a note block for entry 473.
{% /note %}

- Point one for entry 473
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 474

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 474
>
> > Nested quote level two for entry 474
>
> > > Nested quote level three for entry 474

{% note %}
This is a note block for entry 474.
{% /note %}

- Point one for entry 474
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 475

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 475
>
> > Nested quote level two for entry 475
>
> > > Nested quote level three for entry 475

{% note %}
This is a note block for entry 475.
{% /note %}

- Point one for entry 475
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 476

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 476
>
> > Nested quote level two for entry 476
>
> > > Nested quote level three for entry 476

{% note %}
This is a note block for entry 476.
{% /note %}

- Point one for entry 476
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 477

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 477
>
> > Nested quote level two for entry 477
>
> > > Nested quote level three for entry 477

{% note %}
This is a note block for entry 477.
{% /note %}

- Point one for entry 477
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 478

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 478
>
> > Nested quote level two for entry 478
>
> > > Nested quote level three for entry 478

{% note %}
This is a note block for entry 478.
{% /note %}

- Point one for entry 478
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 479

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 479
>
> > Nested quote level two for entry 479
>
> > > Nested quote level three for entry 479

{% note %}
This is a note block for entry 479.
{% /note %}

- Point one for entry 479
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 480

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 480
>
> > Nested quote level two for entry 480
>
> > > Nested quote level three for entry 480

{% note %}
This is a note block for entry 480.
{% /note %}

- Point one for entry 480
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 481

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 481
>
> > Nested quote level two for entry 481
>
> > > Nested quote level three for entry 481

{% note %}
This is a note block for entry 481.
{% /note %}

- Point one for entry 481
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 482

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 482
>
> > Nested quote level two for entry 482
>
> > > Nested quote level three for entry 482

{% note %}
This is a note block for entry 482.
{% /note %}

- Point one for entry 482
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 483

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 483
>
> > Nested quote level two for entry 483
>
> > > Nested quote level three for entry 483

{% note %}
This is a note block for entry 483.
{% /note %}

- Point one for entry 483
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 484

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 484
>
> > Nested quote level two for entry 484
>
> > > Nested quote level three for entry 484

{% note %}
This is a note block for entry 484.
{% /note %}

- Point one for entry 484
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 485

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 485
>
> > Nested quote level two for entry 485
>
> > > Nested quote level three for entry 485

{% note %}
This is a note block for entry 485.
{% /note %}

- Point one for entry 485
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 486

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 486
>
> > Nested quote level two for entry 486
>
> > > Nested quote level three for entry 486

{% note %}
This is a note block for entry 486.
{% /note %}

- Point one for entry 486
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 487

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 487
>
> > Nested quote level two for entry 487
>
> > > Nested quote level three for entry 487

{% note %}
This is a note block for entry 487.
{% /note %}

- Point one for entry 487
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 488

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 488
>
> > Nested quote level two for entry 488
>
> > > Nested quote level three for entry 488

{% note %}
This is a note block for entry 488.
{% /note %}

- Point one for entry 488
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 489

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 489
>
> > Nested quote level two for entry 489
>
> > > Nested quote level three for entry 489

{% note %}
This is a note block for entry 489.
{% /note %}

- Point one for entry 489
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 490

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 490
>
> > Nested quote level two for entry 490
>
> > > Nested quote level three for entry 490

{% note %}
This is a note block for entry 490.
{% /note %}

- Point one for entry 490
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 491

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 491
>
> > Nested quote level two for entry 491
>
> > > Nested quote level three for entry 491

{% note %}
This is a note block for entry 491.
{% /note %}

- Point one for entry 491
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 492

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 492
>
> > Nested quote level two for entry 492
>
> > > Nested quote level three for entry 492

{% note %}
This is a note block for entry 492.
{% /note %}

- Point one for entry 492
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 493

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 493
>
> > Nested quote level two for entry 493
>
> > > Nested quote level three for entry 493

{% note %}
This is a note block for entry 493.
{% /note %}

- Point one for entry 493
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 494

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 494
>
> > Nested quote level two for entry 494
>
> > > Nested quote level three for entry 494

{% note %}
This is a note block for entry 494.
{% /note %}

- Point one for entry 494
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 495

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 495
>
> > Nested quote level two for entry 495
>
> > > Nested quote level three for entry 495

{% note %}
This is a note block for entry 495.
{% /note %}

- Point one for entry 495
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 496

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 496
>
> > Nested quote level two for entry 496
>
> > > Nested quote level three for entry 496

{% note %}
This is a note block for entry 496.
{% /note %}

- Point one for entry 496
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 497

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 497
>
> > Nested quote level two for entry 497
>
> > > Nested quote level three for entry 497

{% note %}
This is a note block for entry 497.
{% /note %}

- Point one for entry 497
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 498

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 498
>
> > Nested quote level two for entry 498
>
> > > Nested quote level three for entry 498

{% note %}
This is a note block for entry 498.
{% /note %}

- Point one for entry 498
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 499

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 499
>
> > Nested quote level two for entry 499
>
> > > Nested quote level three for entry 499

{% note %}
This is a note block for entry 499.
{% /note %}

- Point one for entry 499
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 500

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 500
>
> > Nested quote level two for entry 500
>
> > > Nested quote level three for entry 500

{% note %}
This is a note block for entry 500.
{% /note %}

- Point one for entry 500
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 501

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 501
>
> > Nested quote level two for entry 501
>
> > > Nested quote level three for entry 501

{% note %}
This is a note block for entry 501.
{% /note %}

- Point one for entry 501
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 502

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 502
>
> > Nested quote level two for entry 502
>
> > > Nested quote level three for entry 502

{% note %}
This is a note block for entry 502.
{% /note %}

- Point one for entry 502
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 503

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 503
>
> > Nested quote level two for entry 503
>
> > > Nested quote level three for entry 503

{% note %}
This is a note block for entry 503.
{% /note %}

- Point one for entry 503
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 504

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 504
>
> > Nested quote level two for entry 504
>
> > > Nested quote level three for entry 504

{% note %}
This is a note block for entry 504.
{% /note %}

- Point one for entry 504
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 505

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 505
>
> > Nested quote level two for entry 505
>
> > > Nested quote level three for entry 505

{% note %}
This is a note block for entry 505.
{% /note %}

- Point one for entry 505
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 506

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 506
>
> > Nested quote level two for entry 506
>
> > > Nested quote level three for entry 506

{% note %}
This is a note block for entry 506.
{% /note %}

- Point one for entry 506
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 507

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 507
>
> > Nested quote level two for entry 507
>
> > > Nested quote level three for entry 507

{% note %}
This is a note block for entry 507.
{% /note %}

- Point one for entry 507
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 508

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 508
>
> > Nested quote level two for entry 508
>
> > > Nested quote level three for entry 508

{% note %}
This is a note block for entry 508.
{% /note %}

- Point one for entry 508
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 509

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 509
>
> > Nested quote level two for entry 509
>
> > > Nested quote level three for entry 509

{% note %}
This is a note block for entry 509.
{% /note %}

- Point one for entry 509
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 510

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 510
>
> > Nested quote level two for entry 510
>
> > > Nested quote level three for entry 510

{% note %}
This is a note block for entry 510.
{% /note %}

- Point one for entry 510
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 511

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 511
>
> > Nested quote level two for entry 511
>
> > > Nested quote level three for entry 511

{% note %}
This is a note block for entry 511.
{% /note %}

- Point one for entry 511
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 512

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 512
>
> > Nested quote level two for entry 512
>
> > > Nested quote level three for entry 512

{% note %}
This is a note block for entry 512.
{% /note %}

- Point one for entry 512
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 513

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 513
>
> > Nested quote level two for entry 513
>
> > > Nested quote level three for entry 513

{% note %}
This is a note block for entry 513.
{% /note %}

- Point one for entry 513
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 514

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 514
>
> > Nested quote level two for entry 514
>
> > > Nested quote level three for entry 514

{% note %}
This is a note block for entry 514.
{% /note %}

- Point one for entry 514
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 515

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 515
>
> > Nested quote level two for entry 515
>
> > > Nested quote level three for entry 515

{% note %}
This is a note block for entry 515.
{% /note %}

- Point one for entry 515
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 516

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 516
>
> > Nested quote level two for entry 516
>
> > > Nested quote level three for entry 516

{% note %}
This is a note block for entry 516.
{% /note %}

- Point one for entry 516
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 517

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 517
>
> > Nested quote level two for entry 517
>
> > > Nested quote level three for entry 517

{% note %}
This is a note block for entry 517.
{% /note %}

- Point one for entry 517
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 518

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 518
>
> > Nested quote level two for entry 518
>
> > > Nested quote level three for entry 518

{% note %}
This is a note block for entry 518.
{% /note %}

- Point one for entry 518
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 519

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 519
>
> > Nested quote level two for entry 519
>
> > > Nested quote level three for entry 519

{% note %}
This is a note block for entry 519.
{% /note %}

- Point one for entry 519
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}

---

## Entry 520

In this entry we reference the author as {% $author.name %} and index {% @index %}.

> Primary note for entry 520
>
> > Nested quote level two for entry 520
>
> > > Nested quote level three for entry 520

{% note %}
This is a note block for entry 520.
{% /note %}

- Point one for entry 520
- Point two with a variable {% $series.topic %}
- Point three with a function {% titleCase($series.topic) %}
