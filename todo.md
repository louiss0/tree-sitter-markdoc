# Tests that are failing
- None. `tree-sitter test` passes.

# What bugs are present
- None known.

# What to do next
- Monitor CI for regressions.
- If adding more nested fence cases, update `test/corpus/03-fenced-code.txt` and rerun `tree-sitter test`.
